{
  "name": "@aituber-onair/example-port-agent-config",
  "version": "0.0.0",
  "private": true,
  "description": "Porting existing character-agent configuration to multiple @aituber-onair/agent bot roles",
  "type": "module",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --noEmit --project tsconfig.test.json",
    "test": "vitest run",
    "start": "node dist/server/server.js"
  },
  "dependencies": {
    "@aituber-onair/agent": "file:../..",
    "@aituber-onair/chat": "file:../../../chat"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.8.3",
    "vitest": "^1.3.1"
  }
}
