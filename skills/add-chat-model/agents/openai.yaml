interface:
  display_name: "Add Chat Model"
  short_description: "Add model support and prep chat->core handoff"
  default_prompt: "Use $add-chat-model to add a provider model in chat, then ask whether to run $sync-core-after-chat-upgrade."
